# Dependencies
node_modules/

# Build output (regenerated on every deploy — never commit)
dist/

# Local secrets — NEVER commit these
.env
.dev.vars

# Wrangler cache
.wrangler/

# TypeScript incremental build cache
*.tsbuildinfo

# Python virtualenv
venv/
.venv/

# Instaloader session cache (legacy — safe to delete these files)
.insta-session-*

# Claude Code local settings
.claude/

# OS / editor noise
.DS_Store
Thumbs.db
*.log
*.local
