# No secrets are required for this project.
#
# Behold feed URLs are plain (non-secret) vars set in wrangler.toml and in the
# Cloudflare Pages dashboard under Settings → Environment variables.
#
# If you ever add a service that requires a secret, put it here and copy the
# file to .dev.vars for local Pages dev (never commit .dev.vars).
