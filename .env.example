# Instagram Graph API tokens
# Each token corresponds to one Instagram account (must be Business or Creator account).
# Get tokens via: https://developers.facebook.com/docs/instagram-api/getting-started
#
# Run `npm run fetch-instagram` after setting these to populate public/data/*.json

INSTAGRAM_TOKEN_ART=your_long_lived_token_for_miketheartguy
INSTAGRAM_TOKEN_BJJ=your_long_lived_token_for_mikethemartialartsguy

# LinkedIn OAuth (Cloudflare Pages Functions + Workers KV)
# Create a LinkedIn Developer App at https://developer.linkedin.com
# Add product: "Sign In with LinkedIn using OpenID Connect"
# Add redirect URI: https://miwil.com/api/linkedin-callback
#
# Set these as Cloudflare Pages secrets (dashboard or `wrangler pages secret put`).
# For local dev, copy to .dev.vars (never commit that file).

LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=
LINKEDIN_AUTH_SECRET=     # random string you choose — keeps /api/linkedin-auth private
